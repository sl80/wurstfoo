<h1>My messages</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  
  <%= render :partial => :message, :collection => @messages.includes(:tags), :locals => {:options => true} %>
</table>


<h1>Messages from my friends</h1>

<% @messages.tags.each do |tag| %>
  <%= link_to tag.name, messages_path(:tag => tag.name) %>
<% end %>
  <%= link_to 'all', messages_path %>

<table>
  <%= render :partial => :message, :collection => @friends_messages.includes(:tags), :locals => {:options => false} %>
</table>

<br />
<%= link_to 'New Message', new_message_path %>
<%= link_to 'Users', users_path %>
